const form=document.getElementById('login-form'),button=document.getElementById('login-button'),status=document.getElementById('login-status');
form.addEventListener('submit',async event=>{event.preventDefault();button.disabled=true;status.textContent='Authenticating…';try{const response=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:form.username.value.trim(),password:form.password.value})});if(!response.ok){let message='Sign-in failed.';try{message=(await response.json()).detail||message}catch{}throw new Error(message)}status.textContent='Authenticated. Opening your workspace…';window.location.replace('/')}catch(error){status.textContent=error.message;button.disabled=false;form.password.select()}});
